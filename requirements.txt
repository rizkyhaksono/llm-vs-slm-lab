# Core scientific
numpy>=1.26
pandas>=2.1
matplotlib>=3.8
seaborn>=0.13

# Deep learning (CPU-only)
# CATATAN: install torch DULU dengan --index-url https://download.pytorch.org/whl/cpu
# sebelum file ini, supaya tidak narik wheel CUDA. Lihat README.md.
torch>=2.2

# HuggingFace stack
transformers>=4.44
datasets>=2.20
tokenizers>=0.19
accelerate>=0.33
evaluate>=0.4
sentencepiece>=0.2.0

# Local quantized inference (GGUF via llama.cpp)
llama-cpp-python>=0.2.85

# API clients (Groq pakai OpenAI SDK dengan base_url override)
openai>=1.40
python-dotenv>=1.0

# Notebook
jupyterlab>=4.0
ipykernel>=6.29

# Convenience
tqdm>=4.66
huggingface-hub>=0.24
scikit-learn>=1.4
